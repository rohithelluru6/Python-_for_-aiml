def find_max_min(lst):
    max=lst[0]
    min=lst[0]
    for num in lst:
        if num > max:
            max=num
        if num < min:
            min=num
    return max,min
c=find_max_min([4,7,9,3,6])
print(c)
